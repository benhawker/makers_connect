Work history

<% if @employers!= nil %>
  <% @employers.each do |employer| %>
    <%= employer.company_name %>
  <% end %>
 <% else %>
   No work history added yet
 <% end %>

<%= link_to "Add a work history", new_user_employers_path(@user.id) %>

